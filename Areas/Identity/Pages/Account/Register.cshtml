@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-4">
        <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <h2>Create a new account.</h2>
            <hr />
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" placeholder="password" />
                <label asp-for="Input.Password">Password</label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="password" />
                <label asp-for="Input.ConfirmPassword">Confirm Password</label>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button id="registerSubmit" type="submit" class="w-100 btn btn-lg btn-primary">Register</button>
        </form>
    </div>
    <div class="col-md-6">
        <section>
            <h3>About Apocalypse Survival Planner</h3>
            <hr />
            <p>The Apocalypse Survival Planner is a tool designed for survival enthusiasts and doomsday preppers. It allows users to create comprehensive survival plans for various apocalypse scenarios.</p>
            <p>Users can manage various plan components such as shelter, food, weapons, vehicles, fuel, power sources, and more. The planner also includes a budget tool and price calculator to estimate the total cost of a plan, as well as a survival calculator tool to estimate the amount of time one can survive under a given scenario.</p>
            <p>Whether you're preparing for the worst or just planning for emergencies, the Apocalypse Survival Planner provides detailed feedback to help you craft the best survival plan possible.</p>
        </section>
    </div>
</div>

@section Styles {
    <style>
        /* Apocalypse-themed background */
        body {
            background: url('https://wallpapercave.com/wp/wp7643331.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #f2f2f2;
            font-family: 'Orbitron', sans-serif;
            text-shadow: 1px 1px 3px #000;
        }

        .container, .form-control {
            background-color: rgba(10, 10, 10, 0.7);
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #333;
        }

        h1, h3 {
            color: #ff3c00;
            text-shadow: 3px 3px 6px #000;
        }

        .btn-primary {
            background-color: #a83232;
            border-color: #751f1f;
            color: #fff;
            font-weight: bold;
            box-shadow: 0 0 10px #751f1f;
            transition: all 0.3s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #751f1f;
            box-shadow: 0 0 20px #ff0000;
        }

        .form-label, .form-control {
            color: #fff;
        }

        .text-danger {
            color: #ff6b6b !important;
        }

        /* Additional vibe */
        .row {
            backdrop-filter: blur(4px);
        }
    </style>
}
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
